# Frontend Environment Variables (Vite)
# These are used in the React app via import.meta.env
# NOTE: Vite requires the VITE_ prefix for environment variables to be exposed to the client

# Required: Your Supabase project URL
# Get this from: Supabase Dashboard → Settings → API → Project URL
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Required: Either use VITE_SUPABASE_ANON_KEY (legacy) OR VITE_SUPABASE_PUBLISHABLE_KEY (new format)
# You only need ONE of these two - use whichever is available in your Supabase dashboard

# Option 1 (Recommended - Legacy format): Anonymous key (JWT format starting with 'eyJ...')
# Get this from: Supabase Dashboard → Settings → API → Project API keys → anon public
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Option 2 (Alternative - New format): Publishable key (starts with 'sb_publishable_...')
# Get this from: Supabase Dashboard → Settings → API → Publishable Keys tab
# VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxx

# Optional: Your Supabase project ID (used only for docs/debugging)
# Extract this from your VITE_SUPABASE_URL (the part before .supabase.co)
VITE_SUPABASE_PROJECT_ID=your-project-id
