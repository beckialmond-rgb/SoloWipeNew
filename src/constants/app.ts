/**
 * Application-wide constants
 * Centralized location for all magic numbers, strings, and configuration values
 */

// Default business name
export const DEFAULT_BUSINESS_NAME = 'My Window Cleaning';

// Time constants (in milliseconds)
export const ONE_MINUTE_MS = 60 * 1000;
export const ONE_SECOND_MS = 1000;

// Timeout delays (in milliseconds)
export const DEFAULT_RETRY_DELAY_MS = 500;
export const QUERY_INVALIDATION_DELAY_MS = 100;
export const TOAST_DELAY_MS = 200;

// Date/Time constants
export const EARNINGS_WEEKS = 8;
export const PAID_JOBS_DAYS = 7;
export const UPCOMING_JOBS_WEEKS = 4;

// Default values
export const DEFAULT_CUSTOMER_PRICE = 20;
export const DEFAULT_FREQUENCY_WEEKS = 4;
export const DEFAULT_FREE_JOBS_LIMIT = 10;
export const DEFAULT_FREE_SMS_LIMIT = 10;

// UI constants
export const MAX_PHONE_NUMBER_LENGTH = 20;
export const MAX_NAME_LENGTH = 200;
export const MAX_ADDRESS_LENGTH = 500;
export const MAX_DESCRIPTION_LENGTH = 500;
export const MAX_PRICE = 100000; // Maximum price in pounds

// Retry constants
export const MAX_RETRY_ATTEMPTS = 3;

// Rate limiting
export const RATE_LIMIT_COOLDOWN_MS = ONE_MINUTE_MS; // 1 minute
export const MAX_FAILED_LOGIN_ATTEMPTS = 5;

// URL validation
export const MAX_URL_LENGTH = 500;





